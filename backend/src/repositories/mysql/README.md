# Repository MySQL (paralleli)

Attivi con `USE_MYSQL_DATABASE=true` (migrazione `npm run migrate:mysql`).

- `users`, `restaurants`, `licenses`, `orders`, `payments`, `closures`, `reports` (`saved_reports`)
- `storni` (`storni_entries`) — step `storni`
- `cassa-shifts` (`cassa_shifts`) — step `cassa-shifts`

Vedi `docs/MYSQL_RAILWAY.md` e `src/config/mysqlPersistence.js`.
